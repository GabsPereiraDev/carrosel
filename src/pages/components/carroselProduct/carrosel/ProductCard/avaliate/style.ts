import styled from "styled-components";
import { AiOutlineStar } from 'react-icons/ai'

interface IValue {
    value: number
}

export const Container = styled.div<IValue>`
    margin-left:0 auto;
    width: 12rem;
`